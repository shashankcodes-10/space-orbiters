kind: PersistentVolume
apiVersion: v1

metadata:
  name: space-pv
  labels:
    app: space-pv

spec:
  storageClassName: manual
  persistentVolumeReclaimPolicy: Retain
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/data    