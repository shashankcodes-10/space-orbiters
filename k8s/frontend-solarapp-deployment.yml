kind: Deployment
apiVersion: apps/v1

metadata:
  name: frontend-solarapp
  namespace: space
  labels:
    app: solar-app

spec:
  replicas: 1
  selector:
    matchLabels:
      app: solar-app

  template:
    metadata:
      labels:
        app: solar-app
    spec:
     containers:
      - name: frontend-solarapp
        image: shashank971/space-orbiters-solar-system:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "20m"
            memory: "50Mi"
          limits:
            cpu: "40m"
            memory: "100Mi"  

        startupProbe:
          httpGet:
            path: /
            port: 80
          failureThreshold: 15
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3  
      


