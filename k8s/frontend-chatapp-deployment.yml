kind: Deployment
apiVersion: apps/v1

metadata:
  name: frontend-chatapp
  namespace: space
  labels:
    app: chat-app

spec:
  replicas: 1
  selector:
    matchLabels:
      app: chat-app

  template:
    metadata:
      labels:
        app: chat-app
    spec:
     containers:
      - name: frontend-chatapp
        image: shashank971/space-orbiters-chatroom:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "20m"
            memory: "50Mi"
          limits:
            cpu: "40m"
            memory: "100Mi"  

        startupProbe:
          httpGet:
            path: /
            port: 80
          failureThreshold: 15
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3  
      


